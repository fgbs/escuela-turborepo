{
  "name": "escuela",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "check-types": "turbo run check-types",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "web:dev": "yarn workspace web dev",
    "web:build": "yarn workspace web build",
    "web:start": "yarn workspace web start",
    "web:lint": "yarn workspace web lint",
    "escuela:dev": "yarn workspace escuela dev",
    "escuela:build": "yarn workspace escuela build",
    "escuela:start": "yarn workspace escuela start",
    "escuela:lint": "yarn workspace escuela lint",
    "admin:dev": "yarn workspace admin dev",
    "admin:build": "yarn workspace admin build",
    "admin:start": "yarn workspace admin start",
    "admin:lint": "yarn workspace admin lint"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "prettier-plugin-tailwindcss": "^0.6.11",
    "turbo": "^2.4.4"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
